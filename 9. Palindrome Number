class Solution {
    public boolean isPalindrome(int x) {
      int result=0,n;
        
        for(n=x;n>0;){
            result=(result*10)+(n%10);
            n=n/10;
        }
        if(result==x)
            return true;
        else
            return false;
    }
}
